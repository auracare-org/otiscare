# Replace with your DB credentials!
DATABASE_URL="postgres://user:password@host:port/db-name"

# Optional: Cloud Run inference endpoints (exposed to browser)
# PUBLIC_BINARY_ENDPOINT="https://pytorch-binary-screening-97937849866.us-central1.run.app/predict"
# PUBLIC_MULTICLASS_ENDPOINT="https://pytorch-multiclass-diagnostic-97937849866.us-central1.run.app/predict"

# Optional: Adjust UI animation speed (1 = default, 0.8 = faster, 1.5 = slower)
# PUBLIC_MOTION_SCALE=1
