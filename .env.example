# Replace with your DB credentials!
DATABASE_URL="postgres://user:password@host:port/db-name"

# Cloud Run inference endpoints (exposed to browser)
PUBLIC_BINARY_ENDPOINT="https://your-binary-endpoint.run.app/predict"
PUBLIC_MULTICLASS_ENDPOINT="https://your-multiclass-endpoint.run.app/predict"

# Optional: Adjust UI animation speed (1 = default, 0.8 = faster, 1.5 = slower)
# PUBLIC_MOTION_SCALE=1
